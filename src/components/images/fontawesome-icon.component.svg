<svg
  aria-hidden="true"
  [class]="classNames()"
  role="img"
  [style]="styleList()"
  [attr.viewBox]="viewBox()"
  xmlns="http://www.w3.org/2000/svg"
>
  <path fill="currentColor" [attr.d]="svgPath()" />
</svg>
